name: Data Sync (Unsplash Photos)

on:
  schedule:
    # Weekly on Sunday at 3am UTC (after unsplash stats sync at 2am)
    - cron: '0 3 * * 0'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run mode (preview without writing)'
        required: false
        default: false
        type: boolean

jobs:
  sync:
    uses: ./.github/workflows/data-sync-template.yml
    with:
      sources: unsplash-photos
      dry_run: ${{ inputs.dry_run || false }}
      run_cleanup: false
      log_artifact_name: sync-unsplash-photos
    secrets:
      CLICKHOUSE_HOST: ${{ secrets.CLICKHOUSE_HOST }}
      CLICKHOUSE_PASSWORD: ${{ secrets.CLICKHOUSE_PASSWORD }}
      UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
      TS_OAUTH_CLIENT_ID: ${{ secrets.TS_OAUTH_CLIENT_ID }}
      TS_OAUTH_SECRET: ${{ secrets.TS_OAUTH_SECRET }}
