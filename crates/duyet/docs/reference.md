# `duyet` command reference

Generated by `duyet docs markdown` from the clap command tree. Do not edit by hand; `cargo test -p duyet` fails when this file is stale and `UPDATE_GOLDEN=1 cargo test -p duyet` rewrites it.

## Command tree

```text
duyet  Read duyet.net from the terminal: posts, notes, series, KB, news, insights, and the duyet agent
duyet posts  Blog posts: list, search, read, open
duyet posts list  List posts, newest first
duyet posts search  Full-text search over titles, tags, and summaries (client side)
duyet posts read  Print one post as terminal Markdown
duyet posts open  Open a post in the browser
duyet notes  Short notes
duyet notes list  List notes, newest first
duyet notes read  Print one note
duyet series  Post series
duyet series list  List series
duyet series read  Show one series and its posts
duyet kb  Knowledge base articles
duyet kb list  List articles
duyet kb search  Full-text search over the article index (client side)
duyet kb read  Print one article as terminal Markdown
duyet news  Daily LLM news digest
duyet news today  Today's digest
duyet images  Download a post's images
duyet images download  Download every image referenced by a post
duyet insights  Site insights and stats
duyet insights overview  Site-wide totals
duyet chat  Chat with the duyet agent
duyet contact  Send a contact message (confirms before sending)
duyet jd  Submit a job description (confirms before sending)
duyet jd submit  Submit a job description from a file or URL
duyet comment  Comment on a post (confirms before sending)
duyet auth  Manage the agent token
duyet auth login  Store an agent token in the OS keychain
duyet auth logout  Remove the stored token
duyet auth status  Report whether a token is available (never prints it)
duyet config  Read and edit the config file
duyet config path  Print the config file path
duyet config show  Print every key with its effective value and where it came from
duyet config set  Set a key (validated before writing)
duyet config unset  Remove a key from the file (no error if it is not set)
duyet config doctor  Lint the file: parse errors, unknown keys, bad values, secret-looking keys, loose permissions
duyet update  Update the binary or check for a newer release
duyet doctor  Check config, cache, endpoints, and token state
duyet version  Print version, target, channel, and commit
duyet completions  Print a shell completion script
duyet docs  Generate man pages, Markdown reference, or the command tree
duyet docs man  Write one roff man page per command
duyet docs markdown  Print the Markdown command reference
duyet docs tree  Print every command path, one per line
```

## `duyet`

```text
Read duyet.net from the terminal: posts, notes, series, KB, news, insights, and the duyet agent

Usage: duyet [OPTIONS] <COMMAND>

Commands:
  posts        Blog posts: list, search, read, open
  notes        Short notes
  series       Post series
  kb           Knowledge base articles
  news         Daily LLM news digest
  images       Download a post's images
  insights     Site insights and stats
  chat         Chat with the duyet agent
  contact      Send a contact message (confirms before sending)
  jd           Submit a job description (confirms before sending)
  comment      Comment on a post (confirms before sending)
  auth         Manage the agent token
  config       Read and edit the config file
  update       Update the binary or check for a newer release
  doctor       Check config, cache, endpoints, and token state
  version      Print version, target, channel, and commit
  completions  Print a shell completion script
  docs         Generate man pages, Markdown reference, or the command tree
  help         Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Global options:
      --json
          Print a JSON envelope instead of human output

  -q, --quiet
          Suppress human output (JSON is still printed)

  -v, --verbose...
          Log HTTP requests to stderr; -vv adds cache decisions and resolved paths

      --no-color
          Disable ANSI colors (NO_COLOR and TERM=dumb are also honored)

      --config <PATH>
          Config file to use instead of the default location

  -y, --yes
          Answer yes to every confirmation

      --no-input
          Never prompt; a needed confirmation exits 5 instead

      --dry-run
          Show what would change without writing or sending anything

      --offline
          Serve from cache only and skip network probes (or DUYET_OFFLINE=1)

      --timeout <SECS>
          HTTP timeout in seconds
          
          [env: DUYET_TIMEOUT=]
          [default: 30]

Output:
  Human-readable by default. --json prints one envelope per command:
    {"ok":true,"schema":"duyet.<name>.v1","data":{...}}
    {"ok":false,"schema":"duyet.error.v1","error":{"code":"..","message":"..","exit_code":N}}
  stdout carries data, stderr carries progress and human errors.

Exit codes:
  0 ok   1 generic   2 usage or not implemented   3 network   4 auth
  5 declined   6 not found   10 update available

Config precedence:
  flags > environment (DUYET_<KEY>) > config file > built-in default. See `duyet config show`.

Examples:
  duyet version --json | jq .data.version
  duyet config set blog_url https://blog.duyet.net
  duyet doctor
  duyet docs tree
```

## `duyet posts`

```text
Blog posts: list, search, read, open

Usage: duyet posts <COMMAND>

Commands:
  list    List posts, newest first
  search  Full-text search over titles, tags, and summaries (client side)
  read    Print one post as terminal Markdown
  open    Open a post in the browser
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/posts-data.json and <blog_url>/posts-content/<key>.json, cached under the cache
dir.

Examples:
  duyet posts list --limit 10
  duyet posts list --category data --json | jq '.data.items[].slug'
  duyet posts search "rust wasm"
  duyet posts read 2024-01-01-hello --raw > hello.md

JSON (duyet.posts.v1):
  list/search:
    {"items":[{"slug":"..","title":"..","date":"YYYY-MM-DD","category":"..","tags":[..],"url":".."}],
     "next_cursor":".."?}
  read:
    {"slug":"..","title":"..","date":"..","markdown":"..","images":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet posts list`

```text
List posts, newest first

Usage: duyet posts list [OPTIONS]

Options:
      --limit <N>
          Maximum number of posts

      --category <C>
          Only posts in this category

      --tag <T>
          Only posts with this tag

  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/posts-data.json and <blog_url>/posts-content/<key>.json, cached under the cache
dir.

Examples:
  duyet posts list --limit 10
  duyet posts list --category data --json | jq '.data.items[].slug'
  duyet posts search "rust wasm"
  duyet posts read 2024-01-01-hello --raw > hello.md

JSON (duyet.posts.v1):
  list/search:
    {"items":[{"slug":"..","title":"..","date":"YYYY-MM-DD","category":"..","tags":[..],"url":".."}],
     "next_cursor":".."?}
  read:
    {"slug":"..","title":"..","date":"..","markdown":"..","images":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet posts search`

```text
Full-text search over titles, tags, and summaries (client side)

Usage: duyet posts search <QUERY>

Arguments:
  <QUERY>
          Words to search for

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/posts-data.json and <blog_url>/posts-content/<key>.json, cached under the cache
dir.

Examples:
  duyet posts list --limit 10
  duyet posts list --category data --json | jq '.data.items[].slug'
  duyet posts search "rust wasm"
  duyet posts read 2024-01-01-hello --raw > hello.md

JSON (duyet.posts.v1):
  list/search:
    {"items":[{"slug":"..","title":"..","date":"YYYY-MM-DD","category":"..","tags":[..],"url":".."}],
     "next_cursor":".."?}
  read:
    {"slug":"..","title":"..","date":"..","markdown":"..","images":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet posts read`

```text
Print one post as terminal Markdown

Usage: duyet posts read [OPTIONS] <TARGET>

Arguments:
  <TARGET>
          Post slug or full URL

Options:
      --raw
          Print the raw Markdown source without terminal rendering

      --images <DIR>
          Also download the post's images into this directory

  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/posts-data.json and <blog_url>/posts-content/<key>.json, cached under the cache
dir.

Examples:
  duyet posts list --limit 10
  duyet posts list --category data --json | jq '.data.items[].slug'
  duyet posts search "rust wasm"
  duyet posts read 2024-01-01-hello --raw > hello.md

JSON (duyet.posts.v1):
  list/search:
    {"items":[{"slug":"..","title":"..","date":"YYYY-MM-DD","category":"..","tags":[..],"url":".."}],
     "next_cursor":".."?}
  read:
    {"slug":"..","title":"..","date":"..","markdown":"..","images":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet posts open`

```text
Open a post in the browser

Usage: duyet posts open <SLUG>

Arguments:
  <SLUG>
          Post slug

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/posts-data.json and <blog_url>/posts-content/<key>.json, cached under the cache
dir.

Examples:
  duyet posts list --limit 10
  duyet posts list --category data --json | jq '.data.items[].slug'
  duyet posts search "rust wasm"
  duyet posts read 2024-01-01-hello --raw > hello.md

JSON (duyet.posts.v1):
  list/search:
    {"items":[{"slug":"..","title":"..","date":"YYYY-MM-DD","category":"..","tags":[..],"url":".."}],
     "next_cursor":".."?}
  read:
    {"slug":"..","title":"..","date":"..","markdown":"..","images":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet notes`

```text
Short notes

Usage: duyet notes <COMMAND>

Commands:
  list  List notes, newest first
  read  Print one note
  help  Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/notes-data.json and <blog_url>/note/<id>.md.

Examples:
  duyet notes list
  duyet notes list --json | jq '.data.items[0]'
  duyet notes read 42

JSON (duyet.notes.v1):
  list: {"items":[{"id":"..","title":"..","date":"YYYY-MM-DD","url":".."}]}
  read: {"id":"..","title":"..","date":"..","markdown":".."}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet notes list`

```text
List notes, newest first

Usage: duyet notes list

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/notes-data.json and <blog_url>/note/<id>.md.

Examples:
  duyet notes list
  duyet notes list --json | jq '.data.items[0]'
  duyet notes read 42

JSON (duyet.notes.v1):
  list: {"items":[{"id":"..","title":"..","date":"YYYY-MM-DD","url":".."}]}
  read: {"id":"..","title":"..","date":"..","markdown":".."}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet notes read`

```text
Print one note

Usage: duyet notes read <ID>

Arguments:
  <ID>
          Note id

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/notes-data.json and <blog_url>/note/<id>.md.

Examples:
  duyet notes list
  duyet notes list --json | jq '.data.items[0]'
  duyet notes read 42

JSON (duyet.notes.v1):
  list: {"items":[{"id":"..","title":"..","date":"YYYY-MM-DD","url":".."}]}
  read: {"id":"..","title":"..","date":"..","markdown":".."}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet series`

```text
Post series

Usage: duyet series <COMMAND>

Commands:
  list  List series
  read  Show one series and its posts
  help  Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/series-data.json.

Examples:
  duyet series list
  duyet series read rust-for-data --json | jq '.data.posts[].slug'

JSON (duyet.series.v1):
  list:
    {"items":[{"slug":"..","title":"..","count":N,"url":".."}]}
  read:
    {"slug":"..","title":"..","description":"..","posts":[{"slug":"..","title":"..","date":".."}]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet series list`

```text
List series

Usage: duyet series list

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/series-data.json.

Examples:
  duyet series list
  duyet series read rust-for-data --json | jq '.data.posts[].slug'

JSON (duyet.series.v1):
  list:
    {"items":[{"slug":"..","title":"..","count":N,"url":".."}]}
  read:
    {"slug":"..","title":"..","description":"..","posts":[{"slug":"..","title":"..","date":".."}]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet series read`

```text
Show one series and its posts

Usage: duyet series read <SLUG>

Arguments:
  <SLUG>
          Series slug

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <blog_url>/series-data.json.

Examples:
  duyet series list
  duyet series read rust-for-data --json | jq '.data.posts[].slug'

JSON (duyet.series.v1):
  list:
    {"items":[{"slug":"..","title":"..","count":N,"url":".."}]}
  read:
    {"slug":"..","title":"..","description":"..","posts":[{"slug":"..","title":"..","date":".."}]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet kb`

```text
Knowledge base articles

Usage: duyet kb <COMMAND>

Commands:
  list    List articles
  search  Full-text search over the article index (client side)
  read    Print one article as terminal Markdown
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <kb_url>/llms.txt for the index and <kb_url>/k/<slug>.md for articles.

Examples:
  duyet kb list --category data
  duyet kb search "materialized view" --json | jq '.data.items[].slug'
  duyet kb read data-lakehouse --raw

JSON (duyet.kb.v1):
  list/search: {"items":[{"slug":"..","title":"..","category":"..","url":".."}]}
  read:        {"slug":"..","title":"..","markdown":"..","links":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet kb list`

```text
List articles

Usage: duyet kb list [OPTIONS]

Options:
      --category <C>
          Only articles in this category

  -h, --help
          Print help (see a summary with '-h')

Source: <kb_url>/llms.txt for the index and <kb_url>/k/<slug>.md for articles.

Examples:
  duyet kb list --category data
  duyet kb search "materialized view" --json | jq '.data.items[].slug'
  duyet kb read data-lakehouse --raw

JSON (duyet.kb.v1):
  list/search: {"items":[{"slug":"..","title":"..","category":"..","url":".."}]}
  read:        {"slug":"..","title":"..","markdown":"..","links":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet kb search`

```text
Full-text search over the article index (client side)

Usage: duyet kb search <QUERY>

Arguments:
  <QUERY>
          Words to search for

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <kb_url>/llms.txt for the index and <kb_url>/k/<slug>.md for articles.

Examples:
  duyet kb list --category data
  duyet kb search "materialized view" --json | jq '.data.items[].slug'
  duyet kb read data-lakehouse --raw

JSON (duyet.kb.v1):
  list/search: {"items":[{"slug":"..","title":"..","category":"..","url":".."}]}
  read:        {"slug":"..","title":"..","markdown":"..","links":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet kb read`

```text
Print one article as terminal Markdown

Usage: duyet kb read [OPTIONS] <SLUG>

Arguments:
  <SLUG>
          Article slug

Options:
      --raw
          Print the raw Markdown source without terminal rendering

  -h, --help
          Print help (see a summary with '-h')

Source: <kb_url>/llms.txt for the index and <kb_url>/k/<slug>.md for articles.

Examples:
  duyet kb list --category data
  duyet kb search "materialized view" --json | jq '.data.items[].slug'
  duyet kb read data-lakehouse --raw

JSON (duyet.kb.v1):
  list/search: {"items":[{"slug":"..","title":"..","category":"..","url":".."}]}
  read:        {"slug":"..","title":"..","markdown":"..","links":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet news`

```text
Daily LLM news digest

Usage: duyet news <COMMAND>

Commands:
  today  Today's digest
  help   Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <news_url>/api/public.

Examples:
  duyet news today
  duyet news today --lang vi
  duyet news today --json | jq '.data.stories[].title'

JSON (duyet.news.v1):
  today:
    {"date":"YYYY-MM-DD","lang":"en|vi",
     "stories":[{"title":"..","summary":"..","url":"..","score":N}]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet news today`

```text
Today's digest

Usage: duyet news today [OPTIONS]

Options:
      --lang <LANG>
          Digest language
          
          [default: en]
          [possible values: en, vi]

  -h, --help
          Print help (see a summary with '-h')

Source: <news_url>/api/public.

Examples:
  duyet news today
  duyet news today --lang vi
  duyet news today --json | jq '.data.stories[].title'

JSON (duyet.news.v1):
  today:
    {"date":"YYYY-MM-DD","lang":"en|vi",
     "stories":[{"title":"..","summary":"..","url":"..","score":N}]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet images`

```text
Download a post's images

Usage: duyet images <COMMAND>

Commands:
  download  Download every image referenced by a post
  help      Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Only images on the post's own origin (blog_url) are fetched unless --allow-external is passed.

Examples:
  duyet images download 2024-01-01-hello --out ./hello-images
  duyet images download 2024-01-01-hello --out ./img --json | jq '.data.files[]'

JSON (duyet.images.v1):
  download:
    {"slug":"..","out":"DIR","files":[{"url":"..","path":"..","bytes":N}],"skipped":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet images download`

```text
Download every image referenced by a post

Usage: duyet images download [OPTIONS] --out <DIR> <POST_SLUG>

Arguments:
  <POST_SLUG>
          Post slug

Options:
      --out <DIR>
          Directory to write into (created if missing)

      --allow-external
          Also fetch images hosted outside blog_url

  -h, --help
          Print help (see a summary with '-h')

Only images on the post's own origin (blog_url) are fetched unless --allow-external is passed.

Examples:
  duyet images download 2024-01-01-hello --out ./hello-images
  duyet images download 2024-01-01-hello --out ./img --json | jq '.data.files[]'

JSON (duyet.images.v1):
  download:
    {"slug":"..","out":"DIR","files":[{"url":"..","path":"..","bytes":N}],"skipped":[".."]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet insights`

```text
Site insights and stats

Usage: duyet insights <COMMAND>

Commands:
  overview  Site-wide totals
  help      Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <api_url>/api/insights/overview.

Examples:
  duyet insights overview
  duyet insights overview --json | jq .data

JSON (duyet.insights.v1):
  overview:
    {"posts":N,"words":N,"first_post":"YYYY-MM-DD","latest_post":"YYYY-MM-DD",
     "categories":[{"name":"..","count":N}]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet insights overview`

```text
Site-wide totals

Usage: duyet insights overview

Options:
  -h, --help
          Print help (see a summary with '-h')

Source: <api_url>/api/insights/overview.

Examples:
  duyet insights overview
  duyet insights overview --json | jq .data

JSON (duyet.insights.v1):
  overview:
    {"posts":N,"words":N,"first_post":"YYYY-MM-DD","latest_post":"YYYY-MM-DD",
     "categories":[{"name":"..","count":N}]}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1443
```

## `duyet chat`

```text
Chat with the duyet agent

Usage: duyet chat [OPTIONS] [PROMPT]

Arguments:
  [PROMPT]
          Message to send; read from stdin when omitted

Options:
      --session <ID>
          Continue an existing session

      --no-stream
          Wait for the full reply instead of streaming

  -h, --help
          Print help (see a summary with '-h')

Talks to the duyet agent at <agents_api_url> (apps/agent-api). Requires a token from `duyet auth
login`
or DUYET_AGENT_TOKEN. Without a prompt, reads one from stdin. Streams tokens to stdout on a TTY;
--json streams NDJSON, one {"schema":"duyet.chat.v1","data":{..}} per line.

Examples:
  duyet chat "what did duyet write about rust wasm?"
  echo "summarize the latest post" | duyet chat --no-stream --json | jq -r .data.text
  duyet chat --session 8f2c "continue"

JSON (duyet.chat.v1):
  {"session":"..","role":"assistant","text":"..","done":true}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1445
```

## `duyet contact`

```text
Send a contact message (confirms before sending)

Usage: duyet contact --name <NAME> --email <EMAIL> --message <MESSAGE>

Options:
      --name <NAME>
          Your name

      --email <EMAIL>
          Reply-to address

      --message <MESSAGE>
          Message body

  -h, --help
          Print help (see a summary with '-h')

Prints the full payload and destination, then asks `Send? [y/N]`. --yes skips the prompt; without
a terminal and without --yes the command exits 5. --dry-run shows the payload and sends nothing.
Sends carry an idempotency key so a retry never double-posts.

Examples:
  duyet contact --name "Ada" --email ada@example.com --message "Hi!"
  duyet contact --name Ada --email ada@example.com --message Hi --yes --json

JSON (duyet.submission.v1):
  {"kind":"contact","id":"..","accepted":true,"idempotency_key":".."}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1448
```

## `duyet jd`

```text
Submit a job description (confirms before sending)

Usage: duyet jd <COMMAND>

Commands:
  submit  Submit a job description from a file or URL
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Prints the full payload and destination, then asks `Send? [y/N]`. --yes skips the prompt; without
a terminal and without --yes the command exits 5. --dry-run shows the payload and sends nothing.

Examples:
  duyet jd submit ./role.md --company Acme --note "remote, EU hours"
  duyet jd submit https://example.com/jobs/123 --yes --json

JSON (duyet.submission.v1):
  {"kind":"jd","id":"..","accepted":true,"idempotency_key":".."}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1448
```

## `duyet jd submit`

```text
Submit a job description from a file or URL

Usage: duyet jd submit [OPTIONS] <SOURCE>

Arguments:
  <SOURCE>
          Path to a text/Markdown/PDF file, or an http(s) URL

Options:
      --company <COMPANY>
          Company name

      --note <NOTE>
          Anything else worth knowing

  -h, --help
          Print help (see a summary with '-h')

Prints the full payload and destination, then asks `Send? [y/N]`. --yes skips the prompt; without
a terminal and without --yes the command exits 5. --dry-run shows the payload and sends nothing.

Examples:
  duyet jd submit ./role.md --company Acme --note "remote, EU hours"
  duyet jd submit https://example.com/jobs/123 --yes --json

JSON (duyet.submission.v1):
  {"kind":"jd","id":"..","accepted":true,"idempotency_key":".."}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1448
```

## `duyet comment`

```text
Comment on a post (confirms before sending)

Usage: duyet comment --body <BODY> <POST_SLUG>

Arguments:
  <POST_SLUG>
          Slug of the post to comment on

Options:
      --body <BODY>
          Comment text (Markdown)

  -h, --help
          Print help (see a summary with '-h')

Prints the full payload and destination, then asks `Send? [y/N]`. --yes skips the prompt; without
a terminal and without --yes the command exits 5. --dry-run shows the payload and sends nothing.
Comments enter a moderation queue and are not shown on the blog until approved.

Examples:
  duyet comment 2024-01-01-hello --body "Great post"
  duyet comment 2024-01-01-hello --body "Typo in section 2" --yes --json

JSON (duyet.submission.v1):
  {"kind":"comment","id":"..","accepted":true,"idempotency_key":".."}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1448
```

## `duyet auth`

```text
Manage the agent token

Usage: duyet auth <COMMAND>

Commands:
  login   Store an agent token in the OS keychain
  logout  Remove the stored token
  status  Report whether a token is available (never prints it)
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

The token lives in the OS keychain (key `agent_token`) or in DUYET_AGENT_TOKEN for CI. It is never
written to the config file and never printed: `status` reports set|unset only.

Examples:
  duyet auth login                 # prompts for the token on a TTY
  duyet auth login --token "$TOKEN"
  duyet auth status --json | jq -r .data.state
  duyet auth logout

JSON (duyet.auth.v1):
  status: {"state":"set|unset","source":"keychain|env|none"}
  login/logout: {"state":"set|unset"}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1445
```

## `duyet auth login`

```text
Store an agent token in the OS keychain

Usage: duyet auth login [OPTIONS]

Options:
      --token <TOKEN>
          Token value; prompted for when omitted

  -h, --help
          Print help (see a summary with '-h')

The token lives in the OS keychain (key `agent_token`) or in DUYET_AGENT_TOKEN for CI. It is never
written to the config file and never printed: `status` reports set|unset only.

Examples:
  duyet auth login                 # prompts for the token on a TTY
  duyet auth login --token "$TOKEN"
  duyet auth status --json | jq -r .data.state
  duyet auth logout

JSON (duyet.auth.v1):
  status: {"state":"set|unset","source":"keychain|env|none"}
  login/logout: {"state":"set|unset"}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1445
```

## `duyet auth logout`

```text
Remove the stored token

Usage: duyet auth logout

Options:
  -h, --help
          Print help (see a summary with '-h')

The token lives in the OS keychain (key `agent_token`) or in DUYET_AGENT_TOKEN for CI. It is never
written to the config file and never printed: `status` reports set|unset only.

Examples:
  duyet auth login                 # prompts for the token on a TTY
  duyet auth login --token "$TOKEN"
  duyet auth status --json | jq -r .data.state
  duyet auth logout

JSON (duyet.auth.v1):
  status: {"state":"set|unset","source":"keychain|env|none"}
  login/logout: {"state":"set|unset"}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1445
```

## `duyet auth status`

```text
Report whether a token is available (never prints it)

Usage: duyet auth status

Options:
  -h, --help
          Print help (see a summary with '-h')

The token lives in the OS keychain (key `agent_token`) or in DUYET_AGENT_TOKEN for CI. It is never
written to the config file and never printed: `status` reports set|unset only.

Examples:
  duyet auth login                 # prompts for the token on a TTY
  duyet auth login --token "$TOKEN"
  duyet auth status --json | jq -r .data.state
  duyet auth logout

JSON (duyet.auth.v1):
  status: {"state":"set|unset","source":"keychain|env|none"}
  login/logout: {"state":"set|unset"}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1445
```

## `duyet config`

```text
Read and edit the config file

Usage: duyet config <COMMAND>

Commands:
  path    Print the config file path
  show    Print every key with its effective value and where it came from
  set     Set a key (validated before writing)
  unset   Remove a key from the file (no error if it is not set)
  doctor  Lint the file: parse errors, unknown keys, bad values, secret-looking keys, loose
          permissions
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Keys (each also readable from the environment variable in parentheses):
  blog_url        (DUYET_BLOG_URL)        base URL of the blog
  kb_url          (DUYET_KB_URL)          base URL of the knowledge base
  api_url         (DUYET_API_URL)         base URL of api.duyet.net
  agents_api_url  (DUYET_AGENTS_API_URL)  base URL of the duyet agent
  news_url        (DUYET_NEWS_URL)        base URL of the news digest
  channel         (DUYET_CHANNEL)         stable|beta
  output          (DUYET_OUTPUT)          human|json
  telemetry       (DUYET_TELEMETRY)       true|false, opt-in; nothing is sent in this release
  update.check    (DUYET_UPDATE_CHECK)    true|false

Precedence: flags > environment > config file > default. The file never holds a secret; the agent
token lives in DUYET_AGENT_TOKEN. Writes are atomic (temp file + rename, mode 0600 on unix).

Examples:
  duyet config path
  duyet config show --json | jq '.data.values.blog_url'
  duyet config set blog_url https://blog.duyet.net
  duyet config set --dry-run channel beta
  duyet config unset blog_url
  duyet config doctor

JSON:
  path (duyet.config_path.v1):
    {"path":"..","exists":true}
  show (duyet.config.v1):
    {"path":"..","exists":true,"values":{"<key>":{"value":"..","source":"default|file|env|flag"}}}
  set/unset (duyet.config_write.v1):
    {"path":"..","key":"..","value":".."?,"changed":true,"dry_run":false,"toml":".."}
  doctor (duyet.config_doctor.v1), exits 1 with code config_invalid when any problem is an error:
    {"path":"..","exists":true,"problems":[{"severity":"warning","message":".."}]}
```

## `duyet config path`

```text
Print the config file path

Usage: duyet config path

Options:
  -h, --help
          Print help (see a summary with '-h')

Keys (each also readable from the environment variable in parentheses):
  blog_url        (DUYET_BLOG_URL)        base URL of the blog
  kb_url          (DUYET_KB_URL)          base URL of the knowledge base
  api_url         (DUYET_API_URL)         base URL of api.duyet.net
  agents_api_url  (DUYET_AGENTS_API_URL)  base URL of the duyet agent
  news_url        (DUYET_NEWS_URL)        base URL of the news digest
  channel         (DUYET_CHANNEL)         stable|beta
  output          (DUYET_OUTPUT)          human|json
  telemetry       (DUYET_TELEMETRY)       true|false, opt-in; nothing is sent in this release
  update.check    (DUYET_UPDATE_CHECK)    true|false

Precedence: flags > environment > config file > default. The file never holds a secret; the agent
token lives in DUYET_AGENT_TOKEN. Writes are atomic (temp file + rename, mode 0600 on unix).

Examples:
  duyet config path
  duyet config show --json | jq '.data.values.blog_url'
  duyet config set blog_url https://blog.duyet.net
  duyet config set --dry-run channel beta
  duyet config unset blog_url
  duyet config doctor

JSON:
  path (duyet.config_path.v1):
    {"path":"..","exists":true}
  show (duyet.config.v1):
    {"path":"..","exists":true,"values":{"<key>":{"value":"..","source":"default|file|env|flag"}}}
  set/unset (duyet.config_write.v1):
    {"path":"..","key":"..","value":".."?,"changed":true,"dry_run":false,"toml":".."}
  doctor (duyet.config_doctor.v1), exits 1 with code config_invalid when any problem is an error:
    {"path":"..","exists":true,"problems":[{"severity":"warning","message":".."}]}
```

## `duyet config show`

```text
Print every key with its effective value and where it came from

Usage: duyet config show

Options:
  -h, --help
          Print help (see a summary with '-h')

Keys (each also readable from the environment variable in parentheses):
  blog_url        (DUYET_BLOG_URL)        base URL of the blog
  kb_url          (DUYET_KB_URL)          base URL of the knowledge base
  api_url         (DUYET_API_URL)         base URL of api.duyet.net
  agents_api_url  (DUYET_AGENTS_API_URL)  base URL of the duyet agent
  news_url        (DUYET_NEWS_URL)        base URL of the news digest
  channel         (DUYET_CHANNEL)         stable|beta
  output          (DUYET_OUTPUT)          human|json
  telemetry       (DUYET_TELEMETRY)       true|false, opt-in; nothing is sent in this release
  update.check    (DUYET_UPDATE_CHECK)    true|false

Precedence: flags > environment > config file > default. The file never holds a secret; the agent
token lives in DUYET_AGENT_TOKEN. Writes are atomic (temp file + rename, mode 0600 on unix).

Examples:
  duyet config path
  duyet config show --json | jq '.data.values.blog_url'
  duyet config set blog_url https://blog.duyet.net
  duyet config set --dry-run channel beta
  duyet config unset blog_url
  duyet config doctor

JSON:
  path (duyet.config_path.v1):
    {"path":"..","exists":true}
  show (duyet.config.v1):
    {"path":"..","exists":true,"values":{"<key>":{"value":"..","source":"default|file|env|flag"}}}
  set/unset (duyet.config_write.v1):
    {"path":"..","key":"..","value":".."?,"changed":true,"dry_run":false,"toml":".."}
  doctor (duyet.config_doctor.v1), exits 1 with code config_invalid when any problem is an error:
    {"path":"..","exists":true,"problems":[{"severity":"warning","message":".."}]}
```

## `duyet config set`

```text
Set a key (validated before writing)

Usage: duyet config set <KEY> <VALUE>

Arguments:
  <KEY>
          Key name, e.g. blog_url or update.check

  <VALUE>
          New value

Options:
  -h, --help
          Print help (see a summary with '-h')

Keys (each also readable from the environment variable in parentheses):
  blog_url        (DUYET_BLOG_URL)        base URL of the blog
  kb_url          (DUYET_KB_URL)          base URL of the knowledge base
  api_url         (DUYET_API_URL)         base URL of api.duyet.net
  agents_api_url  (DUYET_AGENTS_API_URL)  base URL of the duyet agent
  news_url        (DUYET_NEWS_URL)        base URL of the news digest
  channel         (DUYET_CHANNEL)         stable|beta
  output          (DUYET_OUTPUT)          human|json
  telemetry       (DUYET_TELEMETRY)       true|false, opt-in; nothing is sent in this release
  update.check    (DUYET_UPDATE_CHECK)    true|false

Precedence: flags > environment > config file > default. The file never holds a secret; the agent
token lives in DUYET_AGENT_TOKEN. Writes are atomic (temp file + rename, mode 0600 on unix).

Examples:
  duyet config path
  duyet config show --json | jq '.data.values.blog_url'
  duyet config set blog_url https://blog.duyet.net
  duyet config set --dry-run channel beta
  duyet config unset blog_url
  duyet config doctor

JSON:
  path (duyet.config_path.v1):
    {"path":"..","exists":true}
  show (duyet.config.v1):
    {"path":"..","exists":true,"values":{"<key>":{"value":"..","source":"default|file|env|flag"}}}
  set/unset (duyet.config_write.v1):
    {"path":"..","key":"..","value":".."?,"changed":true,"dry_run":false,"toml":".."}
  doctor (duyet.config_doctor.v1), exits 1 with code config_invalid when any problem is an error:
    {"path":"..","exists":true,"problems":[{"severity":"warning","message":".."}]}
```

## `duyet config unset`

```text
Remove a key from the file (no error if it is not set)

Usage: duyet config unset <KEY>

Arguments:
  <KEY>
          Key name

Options:
  -h, --help
          Print help (see a summary with '-h')

Keys (each also readable from the environment variable in parentheses):
  blog_url        (DUYET_BLOG_URL)        base URL of the blog
  kb_url          (DUYET_KB_URL)          base URL of the knowledge base
  api_url         (DUYET_API_URL)         base URL of api.duyet.net
  agents_api_url  (DUYET_AGENTS_API_URL)  base URL of the duyet agent
  news_url        (DUYET_NEWS_URL)        base URL of the news digest
  channel         (DUYET_CHANNEL)         stable|beta
  output          (DUYET_OUTPUT)          human|json
  telemetry       (DUYET_TELEMETRY)       true|false, opt-in; nothing is sent in this release
  update.check    (DUYET_UPDATE_CHECK)    true|false

Precedence: flags > environment > config file > default. The file never holds a secret; the agent
token lives in DUYET_AGENT_TOKEN. Writes are atomic (temp file + rename, mode 0600 on unix).

Examples:
  duyet config path
  duyet config show --json | jq '.data.values.blog_url'
  duyet config set blog_url https://blog.duyet.net
  duyet config set --dry-run channel beta
  duyet config unset blog_url
  duyet config doctor

JSON:
  path (duyet.config_path.v1):
    {"path":"..","exists":true}
  show (duyet.config.v1):
    {"path":"..","exists":true,"values":{"<key>":{"value":"..","source":"default|file|env|flag"}}}
  set/unset (duyet.config_write.v1):
    {"path":"..","key":"..","value":".."?,"changed":true,"dry_run":false,"toml":".."}
  doctor (duyet.config_doctor.v1), exits 1 with code config_invalid when any problem is an error:
    {"path":"..","exists":true,"problems":[{"severity":"warning","message":".."}]}
```

## `duyet config doctor`

```text
Lint the file: parse errors, unknown keys, bad values, secret-looking keys, loose permissions

Usage: duyet config doctor

Options:
  -h, --help
          Print help (see a summary with '-h')

Keys (each also readable from the environment variable in parentheses):
  blog_url        (DUYET_BLOG_URL)        base URL of the blog
  kb_url          (DUYET_KB_URL)          base URL of the knowledge base
  api_url         (DUYET_API_URL)         base URL of api.duyet.net
  agents_api_url  (DUYET_AGENTS_API_URL)  base URL of the duyet agent
  news_url        (DUYET_NEWS_URL)        base URL of the news digest
  channel         (DUYET_CHANNEL)         stable|beta
  output          (DUYET_OUTPUT)          human|json
  telemetry       (DUYET_TELEMETRY)       true|false, opt-in; nothing is sent in this release
  update.check    (DUYET_UPDATE_CHECK)    true|false

Precedence: flags > environment > config file > default. The file never holds a secret; the agent
token lives in DUYET_AGENT_TOKEN. Writes are atomic (temp file + rename, mode 0600 on unix).

Examples:
  duyet config path
  duyet config show --json | jq '.data.values.blog_url'
  duyet config set blog_url https://blog.duyet.net
  duyet config set --dry-run channel beta
  duyet config unset blog_url
  duyet config doctor

JSON:
  path (duyet.config_path.v1):
    {"path":"..","exists":true}
  show (duyet.config.v1):
    {"path":"..","exists":true,"values":{"<key>":{"value":"..","source":"default|file|env|flag"}}}
  set/unset (duyet.config_write.v1):
    {"path":"..","key":"..","value":".."?,"changed":true,"dry_run":false,"toml":".."}
  doctor (duyet.config_doctor.v1), exits 1 with code config_invalid when any problem is an error:
    {"path":"..","exists":true,"problems":[{"severity":"warning","message":".."}]}
```

## `duyet update`

```text
Update the binary or check for a newer release

Usage: duyet update [OPTIONS]

Options:
      --check
          Only report whether an update exists (exit 10 if so)

      --channel <CHANNEL>
          Switch channel in the config and update from it

      --version <X>
          Install this exact version

      --rollback
          Swap the previous binary back

  -h, --help
          Print help (see a summary with '-h')

Reads the channel manifest at https://duyet.net/cli/<channel>.json, verifies SHA256 and the minisign
signature, swaps the binary atomically, and keeps the previous one for --rollback. --check exits 10
when a newer release exists and 0 otherwise.

Examples:
  duyet update --check
  duyet update
  duyet update --channel beta
  duyet update --version 0.2.0
  duyet update --rollback

JSON (duyet.update.v1):
  --check:
    {"current":"0.1.0","latest":"0.2.0","channel":"stable","update_available":true}
  update:
    {"from":"0.1.0","to":"0.2.0","channel":"stable","previous_kept":true}

Status: not implemented yet, tracked in https://github.com/duyet/monorepo/issues/1447
```

## `duyet doctor`

```text
Check config, cache, endpoints, and token state

Usage: duyet doctor

Options:
  -h, --help
          Print help (see a summary with '-h')

Probes every base URL with HEAD (GET on 405), 5 s per probe. An unreachable endpoint is reported,
not fatal: the exit code is 0 whenever the report could be produced. --offline or DUYET_OFFLINE=1
skips the probes. The agent token is reported as set|unset and never printed.

Examples:
  duyet doctor
  duyet doctor --json | jq '.data.endpoints[] | select(.status.kind != "reachable")'
  DUYET_OFFLINE=1 duyet doctor

JSON (duyet.doctor.v1):
  {"config_path":"..","config_exists":true,"cache_dir":"..",
   "endpoints":[{"url":"..","status":{"kind":"reachable","status":200},"latency_ms":N}],
   "agent_token":"set|unset","telemetry":false,"offline":false}
  status.kind is reachable {status}, unreachable {message}, or skipped {reason}.
```

## `duyet version`

```text
Print version, target, channel, and commit

Usage: duyet version

Options:
  -h, --help
          Print help (see a summary with '-h')

Works offline and never touches the network or the cache.

Examples:
  duyet version
  duyet version --json | jq -r .data.version

JSON (duyet.version.v1):
  {"version":"X.Y.Z","target":"<triple>","channel":"stable|beta","commit":"<short sha>"}
```

## `duyet completions`

```text
Print a shell completion script

Usage: duyet completions <SHELL>

Arguments:
  <SHELL>
          Shell to generate for
          
          [possible values: bash, elvish, fish, powershell, zsh]

Options:
  -h, --help
          Print help (see a summary with '-h')

Prints the script to stdout; --json has no effect here.

Examples:
  duyet completions bash > ~/.local/share/bash-completion/completions/duyet
  duyet completions zsh > "${fpath[1]}/_duyet"
  duyet completions fish > ~/.config/fish/completions/duyet.fish
  duyet completions powershell >> $PROFILE
```

## `duyet docs`

```text
Generate man pages, Markdown reference, or the command tree

Usage: duyet docs <COMMAND>

Commands:
  man       Write one roff man page per command
  markdown  Print the Markdown command reference
  tree      Print every command path, one per line
  help      Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

Every artifact is generated from the same clap tree that drives the binary, so it cannot drift from
the real flags. `markdown` and `tree` print to stdout regardless of --json.

Examples:
  duyet docs man --out ./man && man ./man/duyet-config.1
  duyet docs markdown > docs/reference.md
  duyet docs tree | grep posts

JSON (duyet.docs_man.v1, `man` only):
  {"out":"DIR","files":["duyet.1","duyet-posts.1",...]}
```

## `duyet docs man`

```text
Write one roff man page per command

Usage: duyet docs man [OPTIONS]

Options:
      --out <DIR>
          Output directory (created if missing)
          
          [default: man]

  -h, --help
          Print help (see a summary with '-h')

Every artifact is generated from the same clap tree that drives the binary, so it cannot drift from
the real flags. `markdown` and `tree` print to stdout regardless of --json.

Examples:
  duyet docs man --out ./man && man ./man/duyet-config.1
  duyet docs markdown > docs/reference.md
  duyet docs tree | grep posts

JSON (duyet.docs_man.v1, `man` only):
  {"out":"DIR","files":["duyet.1","duyet-posts.1",...]}
```

## `duyet docs markdown`

```text
Print the Markdown command reference

Usage: duyet docs markdown

Options:
  -h, --help
          Print help (see a summary with '-h')

Every artifact is generated from the same clap tree that drives the binary, so it cannot drift from
the real flags. `markdown` and `tree` print to stdout regardless of --json.

Examples:
  duyet docs man --out ./man && man ./man/duyet-config.1
  duyet docs markdown > docs/reference.md
  duyet docs tree | grep posts

JSON (duyet.docs_man.v1, `man` only):
  {"out":"DIR","files":["duyet.1","duyet-posts.1",...]}
```

## `duyet docs tree`

```text
Print every command path, one per line

Usage: duyet docs tree

Options:
  -h, --help
          Print help (see a summary with '-h')

Every artifact is generated from the same clap tree that drives the binary, so it cannot drift from
the real flags. `markdown` and `tree` print to stdout regardless of --json.

Examples:
  duyet docs man --out ./man && man ./man/duyet-config.1
  duyet docs markdown > docs/reference.md
  duyet docs tree | grep posts

JSON (duyet.docs_man.v1, `man` only):
  {"out":"DIR","files":["duyet.1","duyet-posts.1",...]}
```
