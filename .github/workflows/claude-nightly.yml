name: Nightly Analysis

on:
  schedule:
    - cron: '0 2 * * *'  # Every day at 2 AM UTC
  workflow_dispatch:

jobs:
  analyze:
    uses: duyet/github-actions/.github/workflows/claude-nightly-analysis.yml@02472419b3e1a62b37ec0c201cad336d26a680d3
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    secrets:
      api_key: ${{ secrets.OPENROUTER_API_KEY }}
      bot_github_token: ${{ secrets.DUYETBOT_GITHUB_TOKEN }}
