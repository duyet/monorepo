{
  "name": "data-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "sync": "tsx src/commands/sync.ts",
    "sync:all": "tsx src/commands/sync.ts all",
    "sync:wakatime": "tsx src/commands/sync.ts wakatime",
    "sync:cloudflare": "tsx src/commands/sync.ts cloudflare",
    "sync:github": "tsx src/commands/sync.ts github",
    "sync:unsplash": "tsx src/commands/sync.ts unsplash",
    "migrate": "tsx src/commands/migrate.ts",
    "migrate:up": "tsx src/commands/migrate.ts up",
    "migrate:down": "tsx src/commands/migrate.ts down",
    "migrate:status": "tsx src/commands/migrate.ts status",
    "migrate:verify": "tsx src/commands/migrate.ts verify",
    "cleanup": "tsx src/commands/cleanup.ts",
    "cleanup:dry-run": "tsx src/commands/cleanup.ts --dry-run",
    "test": "vitest run",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@clickhouse/client": "^1.12.1",
    "@duyet/config": "*",
    "graphql-request": "^6.1.0"
  },
  "devDependencies": {
    "@duyet/tsconfig": "*",
    "typescript": "^6.0.0"
  }
}
