# Agents UI Rebuild TODO

- [x] Research AI chat UI component directions (AI SDK UI + AI Elements)
- [x] Define scratch UI scope for `apps/agents` root chat screen
- [x] Build new standalone chat UI component from scratch
- [x] Add dynamic hero greeting (time-based + user name when signed in)
- [x] Add `fast` / `agent` mode switch in composer
- [x] Add recommendation chips with authenticated fetch fallback
- [x] Wire send flow to `POST /api/chat` with auth token when available
- [x] Replace root chat export to use the new scratch UI
- [x] Run type-check for `apps/agents`
