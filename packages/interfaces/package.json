{
  "name": "@duyet/interfaces",
  "version": "0.1.0",
  "license": "MIT",
  "types": "src/index.ts",
  "files": [
    "src/**"
  ],
  "scripts": {
    "check-types": "tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "lint": "biome lint ."
  },
  "devDependencies": {
    "@duyet/tsconfig": "*",
    "tsup": "^8.0.0",
    "typescript": "*"
  }
}
