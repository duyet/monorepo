{
  "name": "photos",
  "version": "0.1.0",
  "private": true,
  "browser": {
    "fs": false,
    "os": false,
    "path": false
  },
  "scripts": {
    "config": "bun ../../scripts/sync-app-secrets.ts duyet-photos",
    "build": "bun --bun next build",
    "dev": "bun --bun next dev --turbopack -p 3003",
    "fmt": "bun run fix",
    "lint": "biome lint .",
    "start": "bun --bun next start",
    "deploy": "bun run build && bunx wrangler pages deploy out --project-name=duyet-photos",
    "cf:deploy": "bun run --env-file=../../.env.production --env-file=../../.env.production.local build && bunx wrangler pages deploy out --project-name=duyet-photos --commit-dirty=true",
    "cf:deploy:prod": "bun run --env-file=../../.env.production --env-file=../../.env.production.local build && bunx wrangler pages deploy out --project-name=duyet-photos --commit-dirty=true",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@clickhouse/client": "^1.8.1",
    "@duyet/components": "*",
    "@duyet/libs": "*",
    "@radix-ui/react-dialog": "^1.1.2",
    "cloudinary": "^2.5.1",
    "lucide-react": "^0.563.0",
    "next": "16.1.5",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-masonry-css": "^1.0.16",
    "unsplash-js": "^7.0.15"
  },
  "devDependencies": {
    "@duyet/tailwind-config": "*",
    "@duyet/tsconfig": "*",
    "@types/react": "^19.1.4",
    "typescript": "*"
  }
}
